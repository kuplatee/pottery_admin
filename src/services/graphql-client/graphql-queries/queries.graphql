query GetAllCategories {
  categories {
    id
    names {
      en
      fi
    }
  }
}

mutation CreateCategory($input: CreateCategoryInput!) {
  createCategory(input: $input) {
    id
    names {
      en
      fi
    }
  }
}
